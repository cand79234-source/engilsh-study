# English OS — 研究与设计报告

## 第一部分：开源项目研究

### 1.1 English-IA（https://github.com/ErickCassoli/English-IA）

- **License**：MIT（宽松，可自由复用/修改/商用，需保留版权声明）
- **技术栈**：React 19 + TypeScript + Vite + Tailwind 4；后端 FastAPI + SQLAlchemy + Alembic；数据库 SQLite/PostgreSQL；AI 支持 OpenAI、Gemini、Ollama（本地）
- **核心功能**：上下文感知 AI 英语老师、实时语法纠错、CEFR 分级测试、SM-2 智能闪卡（从错误自动生成）、自适应测验、会话报告与历史、连续打卡、Docker 部署
- **AI 调用**：后端统一 `registry.get_llm()` 多 Provider 抽象；prompt 用 `.poml` 模板管理
- **SRS 实现**：`services/evaluation/srs.py` 标准 SM-2，从错误自动生成闪卡
- **可复用部分**：SM-2 实现逻辑、多 Provider LLM 抽象、POML prompt 模板思路、后端分层（services/evaluation/）
- **仅参考部分**：前端 UI 与产品形态（聊天机器人为主），不符合本项目“进度引擎”定位

### 1.2 Google Bespoke（https://github.com/google/bespoke）

- **License**：Apache 2.0（宽松，可复用，需保留 License/NOTICE/修改声明）
- **技术栈**：Python + 轻量 Web UI；uv 包管理；FFmpeg 用于音频生成；Android 应用
- **核心功能**：语境化词汇学习、把单词放入句子、间隔重复、输出驱动（听说读写四模态）、可下载预生成数据集、完全离线
- **AI 调用**：`bespoke/llm.py` 用 Gemini / OpenRouter / OpenAI 生成卡片；学习阶段无需联网
- **SRS 实现**：自研 `urgency.py`，按 listen/speak/read/write 四模态分别跟踪，用 green_streak × 1.8 计算目标间隔，用 tanh 归一化紧迫度，强调“一张卡片多学一个单元、多模态联动复习”
- **可复用部分**：`urgency.py` 复习调度模型（四模态）、`unit.py` 可学习单元数据结构、卡片构建思路
- **仅参考部分**：项目整体是“卡片生成器 + 学习 App”，没有固定阶段/周/Day 进度

### 1.3 FreeLingo（https://github.com/ArtCC/freelingo）

- **License**：AGPL-3.0（强传染性；网络服务必须开源衍生作品；**不适合闭源/个人不公开源码使用**）
- **技术栈**：Next.js 16 + shadcn/ui；后端 FastAPI + SQLAlchemy async + Alembic；PostgreSQL 16 + Redis 7；TTS/STT；多 Provider LLM
- **核心功能**：CEFR 评估 + 个性化学习路线（4/8/12/16 周）、语法/词汇/阅读/写作课程、AI 导师实时对话、SM-2 闪卡、XP/连续打卡、阶段末测试
- **AI 调用**：后端代理全部外部 AI（Ollama/OpenAI/Anthropic/DeepSeek），前端不直接调用
- **SRS 实现**：`backend/app/services/flashcard_sm2.py` 标准 SM-2；初始 EF=2.5；答错 reset；连续正确按 1→6→interval×EF 递增
- **可复用部分**：无代码可直接复用；只参考 SM-2 逻辑和 CEFR 路线图设计
- **法律注意**：若改其代码并作为 Web 服务使用，必须 AGPL 开源全部源码

### 1.4 选型结论

**以 English-IA 为架构蓝本，代码从零实现，算法参考 English-IA/FreeLingo SM-2 + Bespoke urgency。**

原因：
1. 三个项目都没有你需要的“阶段→主题周→Day 1-7 进度引擎 + 手动调进度不删历史 + 20词/10句/周测闭环”。
2. English-IA MIT 最宽松，Bespoke Apache 2.0 次之；直接复制别人的 React/Next.js 代码反而增加“改成个人 OS”的成本。
3. FastAPI + SQLite + 原生前端最适合“单用户、打开即学、长期保存”。

---

## 第二部分：数据库设计

### 核心原则

**进度位置与历史数据完全分离。**

- `progress` 表只有一条记录 = 当前 阶段/周/Day/last_activity。
- 调整进度只改这条记录，不动 `sentences / errors / reviews / history` 等历史表。

### 表结构

| 表 | 作用 |
|---|------|
| `progress` | 当前位置（单行，可安全修改） |
| `weeks` | 每个主题周：标题、语法、词汇 JSON、主题 |
| `day_items` | 每日学习项掌握程度（词/句/语法） |
| `sentences` | 用户造句 + AI 批改结果 |
| `errors` | 错误库（长期累积，按类型统计） |
| `reviews` | SRS 复习卡：kind、ref_key、prompt、answer、SM-2 参数 |
| `quizzes` | 周测/阶段测试记录 |
| `history` | 学习历史流水（追加式，永不删除） |

---

## 第三部分：页面与交互

### 导航（仅 4+1）

主页、主线、复习、周测、错误、设置。

### 各页面职责

- **主页**：告诉我现在该做什么。显示当前进度、今日主线完成度、今日复习（系统自动注入）、薄弱项、「继续学习」按钮。
- **主线**：今日 20 词 + 10 句造句 + 口语。词点击展开详情并“我记住了”；造句输入后 AI 批改。
- **复习**：主动回忆。看词/提示→自己写句子→判定对错→更新 SRS 间隔。
- **周测**：语法题 + 写作 + 口语 + 词汇；≥75% 通过进入下一周。
- **错误**：薄弱项看板，按类型聚合，点开看历史错误、AI 解释、补课建议。
- **设置**：手动调进度（不删历史）、编辑每周内容（JSON）、AI 配置。

---

## 第四部分：SRS 实现

采用 **SM-2 + Bespoke 风格“今日新学也纳入当天复习”**：

- 新学内容当天即出现在“今日复习”（巩固），之后按 SM-2 间隔到期。
- 答对：reps+1，interval 1→3→7→interval×EF，ease 递增（上限 3.0）。
- 答错：reps 归零，interval 重置为 1 天，ease 下调（下限 1.3），提前重新进入复习。

支持复习类型：词汇、搭配、句子、语法、高频错误。

---

## 第五部分：AI 批改

- 后端调用 OpenAI 兼容 `/chat/completions`，返回结构化 JSON：correct / corrected / errors[]。
- 未配置 key 时降级为本地规则纠错（常见固定搭配、第三人称单数、一般过去时等）。
- 错误自动写入 `errors` 表，并针对错误类型创建 SRS 复习卡。
