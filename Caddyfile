# English OS 反向代理 + 自动 HTTPS（Caddy）
#
# 用法：在 .env 设 DOMAIN=你的域名（A 记录指向本机公网 IP），
#      Caddy 会自动向 Let's Encrypt 申请证书并强制 HTTPS。
#      不填 DOMAIN 则仅监听 :80（HTTP），靠 EOS_TOKEN 口令保护，个人使用足够安全。
#
# {$DOMAIN::80} 含义：取环境变量 DOMAIN，若为空则默认站点地址为 :80（HTTP）。

{$DOMAIN::80} {
    encode gzip
    reverse_proxy app:8000
}
